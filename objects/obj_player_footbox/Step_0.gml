/// @description Insert description here
// You can write your code in this editor
x = obj_player.x;
y = obj_player.y;
/*
#region // horizontal push
if (place_meeting(x,y,obj_gibs)) {
    var _object = instance_place(x,y, obj_gibs);
	with (_object) {
		velocity[1] += obj_player.x_speed;
	}
} #endregion
 
#region // vertical push
if (place_meeting(x,y,obj_gibs)) {
    var _object = instance_place(x,y, obj_gibs);
	with (_object) {
		velocity[2] += obj_player.y_speed;	
	}
} #endregion*/